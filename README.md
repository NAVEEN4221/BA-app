# BA App — Next.js scaffold

This is a responsive app made on Next.js, with backend APIs that serve Login/sign up flows.

Quick start:

1. Install dependencies:

```powershell
npm install
```

2. Run dev server:

```powershell
npm run dev
```

3. If you have a Git remote named `goit`, push the initial commit:

```powershell
git remote get-url goit; git push -u goit main
```
