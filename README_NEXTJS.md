# BA App — Next.js scaffold

This workspace contains a minimal Next.js app.

Quick start:

1. Install dependencies:

```powershell
npm install
```

2. Run dev server:

```powershell
npm run dev
```

3. If you have a Git remote named `goit`, push the initial commit:

```powershell
git remote get-url goit; git push -u goit main
```
